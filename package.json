{
  "name": "h-l-api",
  "version": "1.0.0",
  "description": "Firebase Functions API for a YouTube higher-lower game",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "start": "node server.js",
    "emulators": "npx firebase-tools emulators:start --only functions,hosting,firestore",
    "deploy": "npx firebase-tools deploy --only functions,hosting,firestore",
    "test": "node --check app.js && node --check index.js && node --check server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.14.0",
    "express": "^5.2.1",
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^7.2.3"
  }
}
